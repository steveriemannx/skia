# Generated by the Skia CMake build - do not edit, and do not commit.
#
# include() this file from a consumer (the dui project, for instance) to get
# everything needed to compile and link against this build of Skia:
#
#     include("/path/to/skia/out/llvm.arm64.release/skia-vars.cmake")
#     include_directories(${SKIA_INCLUDE_DIRS})
#     add_compile_definitions(${SKIA_DEFINES})
#     link_directories("${SKIA_LIBRARY_DIR}" ${SKIA_SYSTEM_LIB_DIRS})
#     target_link_libraries(app ${SKIA_LINK_LIBS})
#
# Note: add_compile_definitions takes the bare macro names in SKIA_DEFINES, and
# link_directories() only applies to targets declared after it - so do all of
# this before add_executable()/add_library().
#
# SKIA_LIBRARIES is in link order: keep "-lskia" last.

set(SKIA_INCLUDE_DIRS "@SKIA_EXPORT_INCLUDE_DIRS@")
set(SKIA_DEFINES @SKIA_EXPORT_DEFINES@)
set(SKIA_LIBRARY_DIR "@SKIA_EXPORT_LIBRARY_DIR@")
set(SKIA_LIBRARIES @SKIA_EXPORT_LIBRARIES@)
set(SKIA_SYSTEM_LIBS @SKIA_EXPORT_SYSTEM_LIBS@)
set(SKIA_SYSTEM_LIB_DIRS @SKIA_EXPORT_SYSTEM_LIB_DIRS@)
set(SKIA_LINK_LIBS ${SKIA_LIBRARIES} ${SKIA_SYSTEM_LIBS})

set(SKIA_BUILD_TYPE "@CMAKE_BUILD_TYPE@")
set(SKIA_SHARED_LIBS OFF)
